[profile.default]
src = 'src'
script = 'script'
out = 'out'
libs = ['lib']

['rpc_endpoints']
goerli = "${RU}"

[etherscan]
goerli = { key = "${EK}" }

# See more config options https://github.com/foundry-rs/foundry/tree/master/config